<div>
	{% if app.user %}
		<div>
			<p>
				Comentar como
				<strong>{{ app.user.name }}</strong>
			</p>
			{{ form(form, {'action': path('app_comment_new', {'slug': post.slug})}) }}
			</div>

		{% else %}
			<div class="bg-gray-50 rounded-xl p-6 text-center border-2 border-dashed border-gray-300">
				<svg class="w-12 h-12 text-gray-400 mx-auto mb-3" fill="none" stroke="currentColor" viewbox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
				</svg>
				<p class="text-gray-600">
					<a href="{{ path('app_login') }}" class="text-indigo-600 font-semibold hover:underline">Inicia sesión</a>
					para comentar
				</p>
			</div>
		{% endif %}
	</div>
